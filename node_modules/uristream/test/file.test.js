var uristream = require('../uristream');

uristream('file:///.DS_Store', { end: 29, start: 28})
  .on('meta', function(meta) {
    console.error('meta', meta);
  })
  .on('end', function() {
    console.error('end');
  })
  .pipe(process.stdout);